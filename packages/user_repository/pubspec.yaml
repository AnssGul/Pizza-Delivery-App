name: user_repository
description: The repo that handles the users
publish_to: none
version: 0.0.1


environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^4.19.6
  cloud_firestore: ^4.17.4
  rxdart: ^0.27.7

flutter:
  uses-material-design: true  # Added here

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
